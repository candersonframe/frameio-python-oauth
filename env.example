# Frame.io OAuth Configuration
# Get these values from https://developer.adobe.com/console
# Copy this file to .env and fill in your values

# Your application's Client ID
ADOBE_CLIENT_ID=your_client_id_here

# The redirect URI registered in your app (custom URL scheme format)
# Format: adobe+{unique_id}://adobeid/{client_id}
ADOBE_REDIRECT_URI=adobe+your_unique_scheme://adobeid/your_client_id

# OAuth scopes (space-separated)
ADOBE_SCOPES="email additional_info.roles profile openid offline_access"
